/**
 * What you are going to learn
 * 1. primitive Data types - Number, String, Boolean
 * 2. null and undefined
 * 3. Data Collection - Object and Array
 * 4. use of typeof
 * */

  /* Exercise 2 */
  // null is not belong to any of the type
  // null is not a “reference to a non-existing object” 
  // It’s just a special value which represents “nothing”, “empty” or “value unknown”.
  // undefined is “value is not assigned”.
